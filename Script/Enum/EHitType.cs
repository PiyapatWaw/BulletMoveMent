using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace App.Enum
{
    public enum EHitType
    {
        Environment = 0,
        Enemy = 1,
    }

    public enum EHitAction
    {
        Destroy = 0,
        TakeDamage = 1
    }
}
